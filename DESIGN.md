# 英語学習アプリ 設計メモ

## ゴール

英語の基礎固め。**単語1000語と中学英文法を「深く」理解する。**
丸暗記ではなく、コアにあるイメージから理解して、派生を自分で導けるようにする。

スピーキング要素は入れない（検討したが見送り。末尾参照）。

## 想定利用
スマホ・片手・縦持ち。短時間（1〜3分 × 何回も）＋たまに集中（10〜20分）。

---

## 基本原理：コアイメージ

学びたい3つ（単語 / 中学文法 / 句動詞）は、**コアイメージという1つの原理で全部つながる。**
つながった瞬間に暗記量が激減する。これがアプリの背骨。

### 例：on のコアは「上」ではなく【接触】

    on the wall      壁に接触
    on the ceiling   天井に接触（むしろ下）
    on Monday        時間軸に接触
    turn on          回路が接触 → 通電
    put on           体に接触 → 着る
    go on            接触が続く → 継続
    depend on        寄りかかって接触
    It's on me.      負担が私に乗っている → おごり

暗記8個 → 理解1個。

### 句動詞 = 不変化詞のコア × 動詞のコア

           on          off         up          out         down
        (接触)      (分離)      (上へ)      (内→外)     (下へ)
    ──────────────────────────────────────────────────────────
    put   着る       延期する     建てる      消す       書き留める
    get   乗る       降りる       起きる      抜け出す    落ち込む
    take  引き受ける  脱ぐ/離陸    始める      持ち出す    取り壊す

動詞30 × 不変化詞20 = 600マス。実在するのは300〜400。
「300個の熟語」ではなく「50個のコアの交点」として学ぶ。

### 文法も同じ原理

**過去形 = 距離**（時間の過去、ではない）

    時間的な距離   I went there.          過去のこと
    現実からの距離  If I were you...       仮定法 = 現実じゃない
    心理的な距離   Could you help me?     丁寧 = 相手に踏み込まない

→ Could you? が Can you? より丁寧な理由が一発で説明できる。
  「時制」「仮定法」「丁寧表現」は別単元ではなく同じもの。

**to不定詞 = これから向かう / 動名詞 = すでにある事実**

    stop to smoke     吸うために立ち止まる   （これから吸う）
    stop smoking      タバコをやめる         （すでに吸っていた）
    remember to lock  忘れずに鍵をかける     （これからかける）
    remember locking  かけたのを覚えている   （すでにかけた）

→ 「to不定詞をとる動詞／動名詞をとる動詞」の暗記リストが要らなくなる。

---

## 学習項目

| 柱 | 中身 | 数量 | 役割 |
|---|---|---|---|
| **A. 不変化詞のコア** | on, in, at, to, for, of, with, by, from, out, up, down, off, over, through … | 25 | **最重要。全体の土台** |
| **B. 基本動詞のコア** | get, take, put, come, go, make, have, give, keep, turn, break, run, hold … | 30 | A と掛け算される側 |
| **C. 句動詞** | A × B の交点 | 300〜400 | マス目を埋めていく |
| **D. 中学文法のコア** | 冠詞 / 時制 / 助動詞 / 不定詞・動名詞 / 可算不可算 … を「なぜ」で | 40 | 上の原理と地続き |
| **E. 語彙 1000** | NGSL 上位1000 | 1000 | 下記の濃淡をつける |

### E の濃淡

- **深掘り150語** — 多義語・基本動詞・紛らわしい語（run / take / way / hold、say vs tell、look vs see）
  コアイメージ・ニュアンス差・コロケーションまで踏み込む
- **普通に覚える850語** — `table` `water` `student` 等。コアイメージは不要。
  4択とスペル入力で普通に回す

### 語源について（判断の記録）

語源（spect=見る、port=運ぶ、pre-、trans-）が効くのはラテン語系の抽象語（TOEIC 800以降）。
NGSL 上位1000語は get / take / run のようなゲルマン系が大半で、語源を辿っても役に立たない。
→ **語源はメインの仕組みにしない。** 面白いものだけ「⚡豆知識」として拾う。

---

## 学習順序

**第0章に「掛け算の体験」を置く。**
A（不変化詞25）からいきなり始めると体感が「前置詞の勉強」になり、動機が続かない。
最初に `put on / off / up / out / down` だけを見せ、
300個の熟語が50個のコアに畳まれる瞬間を体験させてから A に潜る。

    第0章  put × 5 で「掛け算」を体験（3分）
      ↓
    A 不変化詞のコア
      ↓
    B 基本動詞のコア  →  C 句動詞（A×B の交点）
      ↓
    D 文法 / E 語彙

---

## アプリ構成：教科書 ＋ 問題集

    📖 教科書    コアイメージ解説。いつでも見返せる
    ✍️ 演習      形式A〜D
    🔤 単語      NGSL 上位1000語。4択2種＋スペル入力
    🔁 復習      演習と単語をまとめて間隔反復（SRS）

**演習の解説と教科書ページは相互リンクさせる。**
「間違えた → なぜか読む → 該当ページへ1タップ」の導線がないと教科書は開かれない。

### 教科書ページの構成（確定）

    コアの図 → 派生用法のリスト → ⚡豆知識

`⚡` は「知ってたら面白いよね」の置き場所。学習の楽しさエッセンスとして少量入れる。

**先頭に派生へのジャンプ用チップ列を置く（sticky）。**
on のページは実測 3,391px ＝ スマホ4画面分。初回に読む分には妥当だが、
見返すときに長すぎる。本文は一切削らず、チップ列で見返しやすさだけを足す判断。
スクロールに追従して現在地のチップが光る。
（検討した「派生を折りたたむ」案は、初回の読み物としての流れが失われるため不採用）
（例：なぜ電車は get on で車は get in なのか／on fire の on は「上」ではない）

### 出題形式（確定・全て1ステップ）

    【選択式】
     A コア適用   未知の用法 → どのイメージか（4択）  ← 深さを測る主役
     B 意味選択   句動詞・単語の意味（4択）
     C 用法穴埋め 前置詞 / to不定詞・動名詞 / 冠詞 / 時制（4択）
    【入力式】
     D スペル入力 日本語 → 英語をタイプ（850語向け）

**誤答選択肢の作り方が全て。** ランダムな誤答は消去法で解けてしまい、勘しか測れない。
誤答は必ず「コアを取り違えたときに選んでしまうもの」にする
（on の問題の誤答は in / at にする、など）。

**「なぜ」は出題ではなく解説で届ける。**
2段階の誤用判定は検討したが、テンポを落とすため不採用。
形式A が1ステップで理解そのものを測れているので、役割は足りている。

---

## 出題サンプル

### 形式A — コア適用（教科書に出てこなかった用法を出す）

    Dinner's on me tonight.
    この on はどのイメージ?

     1. 面にくっついている（物理的な接触）
     2. 寄りかかって支えられている      ← 正解
     3. 時間軸の一点に触れている
     4. 途切れずに続いている

    ✓ 費用が私に乗っかっている = 私が持つ。depend on と同じ「寄りかかる on」。

### 形式C — 用法穴埋め

    I'll get ___ the train at Shibuya.
        on     in     at     to

    ✓ on ── 電車は床の上に立てる = 面に乗る。車なら get in（囲まれる）。

---

## 検討して見送った案

- **瞬間英作文（日本語→英語を口に出す）** — スピーキング向けには有効だが、
  今回のゴールが基礎固めに変わったため不採用。将来の拡張候補。
- **音声認識による発話判定** — 声を出せない場面で使えない、認識精度がストレス。
- **2段階の誤用判定** — 深さは出るがテンポを落とす。形式Aで代替。

---

# 実装状況

## 第5弾（完了）── 設定と UI/UX

### 設定
見た目を自分で決められるようにした。どの画面の見出しからも開ける。

    明るさ      自動 / ライト / ダーク
    文字の大きさ 小 / 標準 / 大 / 特大
    色          土 / 藍 / 苔 / 菫 / 紅
    1回に出す数  5 / 10 / 20
    振動        正解・不正解のときに短く震わせる（オン/オフ）
    記録の消去  確認をはさむ。設定は残す

実装で決めたこと。

- **配色は「明るい側」を全部書き、暗い側は必要な変数だけ上書きする。**
  自動は `@media (prefers-color-scheme:dark) :root:not([data-theme="light"])`、
  固定は `:root[data-theme="dark"]`。この2本立てで3通りを賄っている
- **薄い面の色は混色で作る。** `color-mix(in srgb, var(--accent) 13%, var(--bg))`。
  アクセントを変えるだけで薄い面も一緒に付いてくるので、色ごとに
  2つずつ定義する必要がない。混色が使えない環境のために既定値も置いた
- **文字サイズは rem で一括。** `html{font-size: calc(16px * var(--fs))}` にし、
  CSS の font-size を全部 rem に変えた（67か所）。余白は px のままなので、
  レイアウトは崩れず文字だけが大きくなる。
  タブのラベルと俯瞰マップのラベルだけは px のまま据え置いた（枠に収める必要があるため）
- **設定は最初の描画より前に当てる。** `<head>` の小さなスクリプトで
  localStorage を読んで属性を付ける。あとから当てると一瞬もとの見た目が出る

### 単語テストを1画面に収めた
答え合わせのあと「次へ」を押すのに下までスクロールが要る、という状態だった。
実測で最大 204px はみ出していた。

    4択を2×2にする                縦4つは高さを食いすぎる
    問題文と答えの重複をなくす      英→日では問題文に単語が出ているので繰り返さない
    余白と文字を詰める
    「次へ」を下に貼りつける        例文がいくつ出ても必ず押せる

結果、意味が1〜2つの語は完全に1画面に収まる。
意味が3つ以上の語は、端末のノッチとホームバーがある場合に 100px ほど
足りないが、読み進めるためのスクロールであって、
先へ進むための操作は常に画面内にある。

### 句動詞269個すべてを演習にした

マトリクスは**眺めるだけの表**だった。
数えたら、269句動詞のうち **254個が演習に一度も出てこない**。
最初に「句動詞のような複数単語で意味を持つものも学びたい」と言われた部分が、
覚える手段を持っていなかった。

269個に1問ずつ、3つの形を回して作った。

    不変化詞を選ぶ 135   I get ___ at six.（起きる・立ち上がる）
    意味を選ぶ      67   I can't get over it. → 克服 / 降りる / 出回る / 仲良く
    動詞を選ぶ      67   I ___ on the wrong train. → got / came / carried / had

**誤答は必ず「同じ動詞の別の不変化詞」か「同じ不変化詞の別の動詞」から引く。**
無関係な語を混ぜると消去法で解けてしまい、コアを考えなくなる。

**動詞を選ぶ問題では、誤答も抜いた語と同じ活用形にそろえた。**
最初は原形で並べていたが、`I ___ on the wrong train.` に `get` を入れると
時制が合わない。文法で答えが割れると、コアではなく時制を問う問題になる。
30動詞の活用表を持たせ、例文から抜いた語がどの形かを判定して、
誤答を同じ形に変換している。

問題文には日本語の意味を添えてある。
`They ___ down the old sign.` は take down も cut down も文としては成り立つので、
「（取り外す）」が無いと答えが一つに決まらない。
269個すべてについて、同じ動詞・同じ不変化詞の中で日本語が重複しないことを確かめた。

絞り込みには「句動詞」を足した。
句動詞の問題は解説の飛び先として不変化詞・動詞のページを ref に持つが、
絞り込みでは句動詞だけに属させている。
両方に出すと、不変化詞を選んだつもりが句動詞ばかり出てくることになる。
5つの範囲の合計が全問数と一致することを検証している。

#### ついでに見つかった、もっと大きな問題

作った問題の解説が長いので、答え合わせの画面を測った。
**1127問すべてで「次へ」が画面の外にあった。** 最大で219px 下。

単語テストでは以前これを直していた（「次へ」を下に貼りつけ、読み物はその下）。
同じ作りが演習には入っていなかった。
測るまで気づかなかったのは、演習は「答えたら解説を読むもの」だと
思い込んでいて、読まずに進む人の手数を数えていなかったから。

演習も同じ `.sticky-next` にした。
解説は下へ送れば読めるが、読まずに進むのにスクロールは要らない。

### イメージ図を357語まで／補助関数をグローバルから外した

単語のイメージ図を261語から357語へ。1000語の3語に1語になった。

今回いちばん効いたのは、**1語ずつ別の絵を描かない**という方針。

**同じ軸の上で比べる**
past / present / future は、1本の時間軸のどこを太くするかだけが違う。
moment は幅のない一点、period は始まりと終わりのある区間、century は10年×10。
begin と end は同じ線の両端、continue はその線が途切れてまた続く形。
別々に描くと、語どうしの関係が消える。

**同じ入れ物で比べる**
all / some / many / few / most / least / more / less は、
同じ円や同じ棒グラフの中身だけを変える。
good と bad、true と wrong は、左右を入れ替えただけの同じ図にした。
「どちらの側に立っている語か」が一目で分かる。

このほか、気持ち7語（love happy fear hope wish dream worry）、
伝える9語（speak talk tell ask hear sign report news article）、
力5語（power energy force military war）、分野10語、作る7語、仕組み7語。

描き直しは5点。
law が judge と同じ天秤の絵になっていた。law は条文そのものにした。
more に途中で切れた括弧、close に読めない横線が残っていた。
cold の雪の結晶が温度計から離れて隅に浮いていた。
grow の葉が枠の上へ8px はみ出していた。

#### 補助関数をグローバルから外した

図を測るテストが、何も出力せずに落ち続けた。
原因は、テスト側で `var SP2` と書いたこと。
`SP2` は vocab-art.js が `const` で宣言している名前で、
同名の再宣言は**構文エラーになる**。
構文エラーなので try/catch にも掛からず、スクリプトごと無言で死ぬ。

vocab-art.js は `A` `SP2` `LB` を、diagrams.js は `SVG` `CAP` `SPLIT` を、
どれも素の `const` でグローバルに置いていた。
`A` のような1文字の名前が全ファイルから見えている状態で、
将来どれか1つでもぶつかれば、**アプリ全体が無言で止まる**。

両ファイルを即時実行関数で包み、外に出すのを `VOCAB_ART` と `DIAGRAMS`
の1つずつだけにした。
テストでは `window.A` などが undefined であることを毎回確かめている。

いま全ファイルが作るグローバル名を数えると重複は0件。
この点検自体を検証に組み込んだ。

### イメージ図を261語まで増やした／コア図107枚を点検した

単語のイメージ図を201語から261語へ。1000語の4語に1語になった。

**役割の語**（police doctor teacher student worker leader president
officer manager customer member staff employee author judge agent）
どれも「人」の絵になってしまうので、**どこに立っているか**で描き分けた。
leader は旗を持って先頭、manager は組織図の上、member は輪の内側、
agent は二者のあいだ、customer は店へ向かう側、teacher は黒板の前。
訳語を並べるより、位置のほうが役割を伝える。

**場所の語**（hotel church college university restaurant market
office site region country land）

**抽象の語**（step track target pressure growth limit range unit
total average）は、訳語が同じでも図にすると別物になる。
limit は壁とその先の✕、range は端から端までの幅、
pressure は上から押さえる矢印、average は棒グラフを横切る破線。

そのほか動作15語、もの8語。

2枚組が67語になったので、図と例文の対応をまた取り直した。
oil bar cross press の4語に例文を足し、
図の左右がつながっている理由が面白い6語（press plant bar country
office cross）に豆知識を書いた。

描き直しは8点。
leader は星が頭にかぶって塊に見えた。president は演台が頭上のひさしに見えた。
dress はシャツに見えた。track の枕木がゆがんでレンズ状になった。
fix の火花、attack の矢じり脇の線、limit の「く」の字は、
どれも意味のない線として残っていた。bill の紙幣はコインに見えた。

#### コア図107枚の点検

単語の図を見直したついでに、教科書のコア図も同じ物差しで測った。
こちらは作って以来、絵として見直していなかった。

- 107ページすべてに図があり、使われていない図もない
- viewBox からのはみ出しは1枚（vLet のラベルが上に2px）。直した
- ラベル同士の重なりは0件
- `var(--text)` を使う図が5枚あったが、すべて `<text>` の文字色で、
  これは正しい使い方。単語図の dark にあったような
  「暗い色のつもりで --text を塗る」バグは無かった

### イメージ図を201語まで増やした

129語から72語足して201語。1000語の5語に1語に絵がついた。

足したのは、**絵にしにくそうで、絵にする値打ちがある語**を優先した。

**位置の語**（middle side center above below behind across along toward beyond ahead）
訳語の「〜の方へ」を読むより、基準と矢印を見るほうが速い。
toward には〈着くとは限らない〉、beyond には〈仕切りの向こう側〉と添えた。
above と below は、線に触れていないことが要点なので破線で示す。

**時間の語**（minute hour month season summer evening afternoon tomorrow yesterday weekend）
yesterday と tomorrow は同じ時間軸に3日を並べ、どれを指すかだけを塗り分ける。
2語を別々に描くより、同じ図の塗り分けにしたほうが関係が見える。
minute と hour は長針と短針で描き分けた。

**家族の語**（father mother son daughter brother sister wife husband）
誰から見た誰なのかを、線と矢印と濃淡で示す。
son と daughter は「親から子へ」の矢印、brother と sister は上の括弧で同じ親を示す。
訳語だけだと向きが分からない。

このほか動作20語（sit stand sleep eat drink laugh smile sing dance fly
drop draw wear read write buy sell pay reach touch）と、
もの8語、抽象7語。
laugh と smile は〈声を出す／出さない〉、buy と sell は同じ図の矢印を逆にした。

語源が面白い9語に豆知識も足した。
month と moon、husband の中の「家」、wife と woman、friend と free、
write の「引っかく」、read の「解き明かす」、draw が全部「引く」であること。

描き直したのは11点。
ring は石を輪の中に描いてしまい顔のように見えた。
link は2つの輪が重なってプレッツェルになった。
fly は飛行機の形が崩れて読めず、紙飛行機にした。
draw は鉛筆だけで、肝心の描かれた線が無かった。
rock はテントに見えた。sit は人が座って見えなかった。
huge・behind・child には意味のない線や点が残っていた。

### イメージ図を129語まで増やした

最初は65語。描いて意味があるものを足していって129語になった。

描く対象の選び方は3つ。

**意味が2つある語** — 図を2枚並べると、なぜ1語に同居しているかが見える。
arm（腕／武器）、store（店／蓄える）、field（野原／分野）、
ground（地面／根拠）、park（公園／駐車する）、space（空き／宇宙）。
右のラベルには、つながりを一言添える。
field なら〈守備範囲〉、ground なら〈土台〉。

**反対語がある語** — 同じ2枚組でも、右に置くのは反対語のほう。
big↔small、long↔short、hot↔cold、new↔old、strong↔weak。
1語だけ見るより輪郭がはっきりする。反対語には英語も併記した。

**具体物** — bed、card、machine、station、village のように、
日本語訳を読むより絵を見るほうが速いもの。

2枚組が50語になったところで、図と例文がずれていることに気づいた。
図が2つの意味を約束しているのに、例文は1つしかない語があった。
図を足したぶん例文も足して、**2枚組の語には必ず2つ以上の例文がある**状態にした。
豆知識も、つながりが面白い10語（arm、store、park、train、fall など）に足した。

描き直したものもある。

- `dark` は「暗い」を `var(--text)` で塗っていた。
  ダークテーマでは --text が明るい色なので、**「暗い」が白い箱になっていた。**
  三日月と太陽に描き替えた。どちらのテーマでも意味が反転しない。
- `station` が `train` とほぼ同じ絵だった。駅舎・ホーム・線路に描き替えた。
- `arm` の「武器」が矢印で読めなかった。盾と剣にした。
- `strong` は腕の筋肉を描いたが読めなかった。
  バーベルを持ち上げられるか／潰れるか、にした。
- `fill` はラベルが容器に重なっていた。`train` はラベルが枠から切れていた。

検証は SVG の実測で自動化している。
129点すべての描画範囲を `getBBox()` で測り、
viewBox（240×140）からはみ出していないか、ラベル同士が重なっていないかを見る。
`table` と `paper` のラベルが枠を6px超えていたのはこれで見つけた。

### スクロール位置の扱い
render() が毎回 #view を先頭に戻していたので、
単語帳で行を開くだけでも画面が上まで飛んでいた。
演習で答えを選んだときも同じことが起きていた。

「いまどの画面を見ているか」を表す鍵（screenKey）を作り、
鍵が変わらない更新は同じ画面の中の出来事とみなして位置を保つようにした。

    鍵に入れる      タブ / 開いているページ / ジャンル / 検索語 /
                   絞り込み / 帯 / 何問目か
    鍵に入れない    単語帳の開閉 / マトリクスの選択 /
                   答えたかどうか / 設定の開閉

innerHTML を入れ替えると中身ごと作り直されてスクロールが 0 に戻るので、
鍵が同じときは元の位置を明示的に書き戻している。

あわせて、鍵ごとに位置を覚えるようにした。
一覧からページを開いて戻ったとき、前に見ていた場所から始まる。
107ページや333語の一覧では、これが無いと毎回探し直しになる。

### 単語帳
テストとは別に、1000語をただ眺めて調べられる場所を作った。
覚えているかを問われずに、意味・例文・豆知識・絵を見に行ける。

    帯で区切る          1〜333 / 334〜666 / 667〜1000
    検索               単語・訳・例文から。検索中だけ帯をまたぐ
    絞り込み            すべて / 未出題 / 間違えた / 正解済み / 絵あり
    行をタップして展開    絵・発音記号・意味ごとの例文・豆知識・コアページ

左端の順位バッジの色で、正解済み（緑）と間違えた（赤）が一覧のまま分かる。

### イメージ図
インラインSVGで65語ぶん描いた。教科書のコア図と同じ方式なので、
CSS変数を参照していて、明暗のテーマにもアクセント色の変更にも自動で追従する。
画像ファイルではないので、拡大しても粗くならず、オフラインでも当然そのまま出る。

意味が2つある語は2枚並べて違いを見せる方式にした。
bank（銀行／土手）、court（裁判所／コート）、second（時計の最小単位／2番目）、
patient（患者／耐えている）など。
訳を読むより、2枚の絵を見比べるほうが速い。

テストの答え合わせでは、絵も豆知識と同じく「次へ」の下に置いている。
どちらも「読みたい人だけが見るもの」で、性質が同じだから。

### 豆知識の置き場所
単語の豆知識は「次へ」ボタンの**下**に出す。

    上   問題文・選択肢・答え・例文
    ──   ここまでが1画面に収まる
    次へ  画面下端に貼りつく（常に押せる）
    下   ⚡豆知識

こうすると、スクロールの意味が変わる。
上に置くと「先へ進むために下まで送る」作業になるが、
下に置けば「読みたいから送る」になり、読まない人は何もしなくてよい。
必須の操作と、あってもなくてもよい読み物を、位置で分けている。

入れたのは64語。知ると意味のつながりが腑に落ちるものだけにした。
patient が「患者」と「辛抱強い」の両方なのはどちらも「耐える」だから、
second が「秒」と「2番目」の両方なのは1時間を2回割った「第二の小部分」だから、など。
由来が俗説のもの（news が東西南北の頭文字、など）は入れていない。

豆知識で「2つの意味」を説明している語は、例文も2つ出るようにしてある
（patient・bank・court・stock など13語を後から足した）。
説明と例文が食い違うと、かえって分かりにくいため。

### 意味ごとの例文
1つの訳に複数の意味が入っている語に、意味の数だけ例文を付けた（199語・509例文）。

対象の選び方が肝で、訳に「・」が入る語は1000中692語あるが、
その多くは「すでに・もう」のような言い換えにすぎない。
足しても水増しになるので、**本当に別の意味を持つ語だけ**に絞った。

    意味3つ以上の110語   全部（course 講座/進路/もちろん、turn 回る/曲がる/順番 など）
    意味2つの76語        別物のものだけ（see 見る/わかる、interest 興味/利子 など）
                         言い換えにすぎないものは入れていない

### UI/UX の見直し

- **教科書をジャンルで切り替え** —— 107ページを一続きにしていたので、
  下まで行くのが大変だった。不変化詞 / 基本動詞 / 文法 / 紛らわしい語 の
  タブにし、件数と進捗バーを添えた。
  タブは sticky にして、リストの途中からでも別のジャンルへ移れるようにした。
  40ページある文法は GRAMMAR_GROUPS で 時制 / 文型と語順 / 動詞の後ろ /
  比較 / つなぐ / その他 に小分けしてある。
  「すべて」は置いていない —— 107件を一度に出すのが元の問題なので、
  横断したいときは検索を使う
- **検索** —— 107ページから引けなかったのを解消。見出し語だけでなく
  コア・派生・例文・豆知識まで横断し、**何にあたったか**も一緒に出す。
  意味の側（「接触」「ほとんど〜ない」）からでも辿り着ける
- **演習の範囲指定** —— すべて／不変化詞／基本動詞／文法／紛らわしい語。
  範囲ごとの進捗バーも出す。「文法だけ集中的に」が回せるようになった
- **続きから** —— 前に見ていたタブとページを覚えて、次回そこから開く。
  ただし演習・単語の途中の1問までは覚えない（途中再開はかえって迷う）
- **手ごたえ** —— 正誤で短く振動させる。正解は12ms、不正解は10-40-10ms
- **読み上げ** —— 正誤の表示に `role="status" aria-live="polite"` を付けた

## 第4弾（完了）── 文法40項目

    107ページ  不変化詞25 ・ 基本動詞30 ・ 文法40 ・ 紛らわしい語12
    858問      演習（文法30ページ分 +240）
    432派生 ・ 894例文 ・ 222本の⚡ ・ 107コア図

文法を10→40に広げ、中学英文法をひと通り網羅した。

    文型と語順 5   語順 / SVOO / SVOC / there is / 命令文・感嘆文
    動詞の後ろ 5   使役 / 知覚動詞 / 不定詞3用法 / 疑問詞+to・間接疑問 / that節
    時制の残り 4   未来表現 / 過去進行形 / 過去完了 / 助動詞+have+過去分詞
    比較       4   比較級 / 最上級 / as〜as / 比較の応用
    つなぐ     5   等位接続詞 / 従属接続詞 / 関係副詞 / 分詞の後置修飾 / 分詞構文
    その他     7   仮定法 / it / 代名詞 / 数量表現 / 付加疑問 / 形容詞副詞の位置 / 複数形と所有格

### 新しいページも、すべて既存のコアへ繋いでいる
- 仮定法 → 過去形＝【距離】を現実に向けただけ
- 最上級の the → 冠詞＝【特定できるか】
- 知覚動詞の原形と -ing → 進行形＝【途中の一コマ】
- 使役の get だけ to がつく → get＝【至る】
- that の省略 → 関係代名詞の目的格の省略と同じ理屈
- in the class / of the three → in＝【囲まれた内側】／of＝【分離】

文法を「単元の集合」ではなく「少数のコアの言い換え」として並べられた点が、
このアプリの設計で一番効いたところ。

## 第3弾（完了）── 語彙

    77ページ    不変化詞25 ・ 基本動詞30 ・ 文法10 ・ 紛らわしい語12
    1000単語    NGSL 頻度順の上位1000語。うち66語はコアページへ繋がる
    618問       演習（紛らわしい語12ページ分 +96）
    312派生 ・ 657例文 ・ 162本の⚡ ・ 77コア図

語彙は「量」と「深さ」を分けて扱った。

- **量** —— NGSL 上位1000語を姉妹プロジェクト ngsl-vocab-quiz から取り込み、
  4択（英→日／日→英）とスペル入力の3モードで回す。
  誤答の選択肢は同じ頻度帯から引き、難易度をそろえている
- **深さ** —— 日本語では同じ訳になる語群（say/tell/speak/talk など12組）を
  教科書ページにした。語源でも丸暗記でもなく、「何で割れているか」を1本の軸で示す

**1000語のうち66語には、すでにコアページがある。**
その語が出題されたとき、答え合わせの画面からコアページへ飛べるようにした。
語彙アプリと教科書を別々に持つのではなく、同じアプリの中で地続きにするための導線。
（不変化詞25・基本動詞30 の55語に加え、冠詞・助動詞・do の11語）

### 紛らわしい語ページが、既存ページと繋がっている例
- wish の後ろが過去形になる → 過去形＝【距離】のページへ
- a few と few が逆になる → 冠詞＝【輪郭のある1つ】のページへ
- remember to / -ing → to不定詞と動名詞のページへ
- It takes two hours. の take → take＝【自分の方へ取る】のページへ
- arrive at と arrive in → at＝【点】／in＝【囲まれた内側】のページへ

## 第2弾（完了）── コア体系の完成

    65ページ   不変化詞25 ・ 基本動詞30 ・ 文法10
    266派生    各ページのコアからの派生用法
    568例文    日本語訳と「なぜそうなるか」のメモつき
    138本の⚡   派生1つあたり 0.52本（on の比率を全体に維持）
    269句動詞  30動詞 × 25不変化詞 = 750マスのうち実在する269
    522問      コア適用261 ・ 用法穴埋め231 ・ 意味選択30
    65コア図   全ページにSVGの模式図

第1弾は 28ページ / 226問 / 60句動詞。第2弾で不変化詞を25、基本動詞を30まで広げた。
「基本動詞を増やす」ことは「深掘り語彙を増やす」こととほぼ同じ作業になる
（深掘りすべき多義語は結局 run / hold / break / keep などの基本動詞そのもの）ため、
この順序を選んだ。

## ファイル構成

    index.html          アプリ本体（ビルド不要・開くだけ）
    style.css           スマホ縦持ち・片手前提。ダークモード自動
    app.js              画面遷移・採点・SRS・マトリクス
    diagrams.js         コア図28点（CSS変数を参照するのでテーマ追従）
    data/particles.js   不変化詞10ページ
    data/verbs.js       基本動詞8ページ
    data/grammar.js     文法10ページ
    data/phrasals.js    句動詞マトリクス
    data/exercises.js   演習226問

## 実装した仕掛け

### 教科書ページ
- 冒頭に **派生へジャンプするチップ列（sticky）**。スクロールに追従して現在地が光る
- `on` ページは実測 3,391px ＝ スマホ4画面分。初回に読む分には妥当だが
  見返すときに長いので、本文を削らずチップ列だけを足した
  （「派生を折りたたむ」案は初回の読み物としての流れが失われるため不採用）

### 演習
- 1画面完結（問題文 → 4択 → 正誤 → 解説 → 次へ）。スクロールなしで回る
- 解説から該当の派生用法へ直接ジャンプし、飛び先が1.8秒ハイライトされる
- `jumpTo` で**ページ横断のリンク**ができる。
  例：`in` のページで on time を答えさせ、解説は `on` のコアへ飛ばす

### 句動詞マトリクス（第2弾でUIを作り直し）
750マスは、第1弾の「全マスを横スクロールで並べる」方式では破綻する。
全体像が見えないと「掛け算」の快感が消えるため、2層構造に変更した。

- **俯瞰マップ** — 30行 × 20列を11pxのマスで描き、750マス全体を1画面に収める。
  ラベルを捨てて密度だけを見せることで、分布そのものが情報になる
  （up / out / off / on の列が濃く、右へ行くほど疎）
- **動詞の行** — チップで動詞を選ぶと、その動詞の句動詞がカードで並ぶ。
  こちらは大きなタップ領域を確保する。俯瞰は「見る」、行は「触る」と役割を分けた
- マスをタップすると開く。**意味を先に推測してから開く**使い方を促す
- 開いたマスは色が変わって埋まっていく（localStorage に保存）
- 詳細に「動詞のコア × 不変化詞のコア」を必ず併記し、両ページへのリンクを置く
- **空白のマスも情報**。from / about / of / under / between は句動詞を1つも作らない
  （前置詞ではあっても、動詞にくっつく副詞辞にはならない）。
  have の行がほぼ空なのも、「圏内にあるだけで動きがない」ため

### 復習
- 間隔反復（0 → 1 → 3 → 7 → 21 → 60日）。誤答で box が 0 に戻る

## 第5弾以降

当初の3本柱（単語1000・中学英文法・句動詞）は、ひと通り揃った。

- 不変化詞 after / behind —— look after / take after / leave behind を
  マトリクスに載せるために必要。今は動詞ページの例文にだけ入っている
- 紛らわしい語の追加（make/do、in time/on time など。既にページ内の豆知識では触れている）
- 語彙を1000語より先へ（NGSL は2801語ある）

## データの出典について（第3弾で追加）

data/vocab.js は NGSL（CC BY 3.0）の上位1000語と、ipa-dict（MIT）の発音記号を含む。
表示義務があるため README に出典表を置き、NOTICE ファイルも用意した。
日本語訳と例文は本プロジェクトで付与したものなので MIT。

元データの癖：the が1位ではなく1000位に置かれ、I が収録されていない。
ngsl-vocab-quiz と同じ並びを保つため直していない。the は冠詞ページで扱う。

---

## 既存資産との関係

- `../ngsl-vocab-quiz` — NGSL 2801語の4択/フラッシュカード。
  語彙の「量」はこちらが担当していたが、上位1000語は本アプリに同居させる
  （850語も「この単語は on と結びつく」等でコア学習と繋がるため、分けると勿体ない）
- `../english-grammar/english_grammar.tex` — 文法をまとめた LaTeX。D の下敷きに使える
